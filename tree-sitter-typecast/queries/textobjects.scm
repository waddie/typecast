; Text objects for typecast scripts

; Comments
(comment) @comment.inside
(comment)+ @comment.around

; Directives
(directive) @class.around

; Type commands
(type_command) @function.around
(type_content) @function.inside

; Special keys
(special_key) @parameter.inside
